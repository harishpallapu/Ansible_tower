---
- name: Running Docker
  hosts: all
  become: true
  gather_facts: false
  tasks:
  
    - name: pull an image
      shell: docker run --rm --name jmeter -w /project -v $PWD:/project harishpallapu/jmeter-3i:latest 
        

